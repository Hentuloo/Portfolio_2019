import React from 'react';
import { storiesOf } from '@storybook/react';
import ProjectBox from './ProjectBox';

storiesOf('ProjectBox', module).add('basic', () => (
  <ProjectBox>sdsd</ProjectBox>
));
